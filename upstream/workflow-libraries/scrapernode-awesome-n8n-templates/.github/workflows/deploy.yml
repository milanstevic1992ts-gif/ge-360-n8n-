name: Deploy Astro to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build Astro
        run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

  seo-ping:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Ping Google Sitemap
        run: curl -s "https://www.google.com/ping?sitemap=https://scrapernode.github.io/awesome-n8n-templates/sitemap.xml"

      - name: Ping Bing Sitemap
        run: curl -s "https://www.bing.com/ping?sitemap=https://scrapernode.github.io/awesome-n8n-templates/sitemap.xml"

      - name: IndexNow - Notify Bing, Yahoo, DuckDuckGo
        run: |
          curl -s -X POST "https://api.indexnow.org/indexnow" \
            -H "Content-Type: application/json" \
            -d '{
              "host": "scrapernode.github.io",
              "key": "d2089c8a0a094a98a52b45a38ab702bf",
              "keyLocation": "https://scrapernode.github.io/awesome-n8n-templates/d2089c8a0a094a98a52b45a38ab702bf.txt",
              "urlList": [
                "https://scrapernode.github.io/awesome-n8n-templates/",
                "https://scrapernode.github.io/awesome-n8n-templates/sitemap-index.xml"
              ]
            }'
